# Package overrides go here.
# Wire into flake.nix via: nixpkgs.overlays = [ (import ../overlays/default.nix) ];
#
# Example:
# final: prev: {
#   myPackage = prev.myPackage.overrideAttrs (old: {
#     version = "1.2.3";
#   });
# }
final: prev: {}
